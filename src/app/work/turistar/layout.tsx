import type { ReactNode } from 'react';

import './work.css';

type TuristarLayoutProps = {
  children: ReactNode;
};

export default function TuristarLayout({ children }: TuristarLayoutProps) {
  return children;
}
